# Ignore files in the build folder
build/*

# Except for the executable
!build/release/
build/release/*
!build/release/key_engine

# Files for watching and building
goodmorning
build-watch
watched
rebuild
